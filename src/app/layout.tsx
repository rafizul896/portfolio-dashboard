import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dashboard | Rafizul Islam Rafiz Portfolio",
  description:
    "Welcome to the portfolio dashboard of Rafizul Islam Rafiz. Manage your blogs, projects, skills, and experiences all in one place. Streamline your personal branding and stay organized with powerful tools.",
  icons: {
    icon: "https://i.ibb.co/bMpJNZ0B/favicon-2.png",
  },
  
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster position="top-center" richColors />
      </body>
    </html>
  );
}
